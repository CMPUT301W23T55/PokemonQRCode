<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/found_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Code found!"
        android:textAlignment="center"
        android:textSize="24sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/found_picture_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Save picture of location?"
        android:textAlignment="center" />

    <ImageButton
        android:id="@+id/found_image_button"
        android:layout_width="match_parent"
        android:contentDescription="Save picture of location?"
        android:layout_height="150dp"
        android:scaleType="fitCenter"
        app:srcCompat="@android:drawable/ic_menu_camera" />

</LinearLayout>